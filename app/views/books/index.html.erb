<%= render "shared/header" %>

<div class="back-img-library">
  <div class="bkRGBA">
    <div class='main'>


      <div class='title-contents-flex'>
        <div class="title-contents-left">
          <h2 class='service-title'>
            図書館
          </h2>
        </div>
        <div class="title-contents-right">
          <div class='create-btn-content'>
            <%= link_to new_book_path, cass: 'create-btn', id: "create-btn" do %>
                <div class="create-btn">新しく図書カードに<br>記録する</div>
            <% end %>
          </div>
        </div>
      </div>

      <div class="book-contents-main">
        <ul class='book-contents'>

          <% @books.each do |book| %> <%# 投稿表示用 %>
            <li class='book-content'>
              <div class="book-post-user-name">
                ［<%= book.created_at.month%>/<%= book.created_at.day %> <%= book.created_at.strftime('%R') %>］
                <%= link_to '#', cass: "book-post-user-link" do %>
                  <span><%= book.user.user_information.nickname %></span>
                <% end %>
                さんの図書カードより
              </div>
              <div class="book-category">
                <%= book.category.name %>
              </div>
              <div class="book-content-center">
                <div class="book-content-left">
                  <div class="book-img-content">
                    <%= image_tag book.image.variant(resize: '82x118'), class: 'book-img' %>
                  </div>
                </div>
                <div class="book-content-right">
                  <div class="book-title"><%= book.book_title %></div>
                  <div class="book-author"><%= book.author %></div>
                  <div class="book-introduction"><%= book.book_introduction %></div>
                </div>
              </div>
              <%= link_to '#', cass: 'book-content-link' do %>
                <div class="read-more">もっと読む ></div>
              <% end %>
            </li>
          <% end %> <%# //投稿表示用 %>
          
        </ul>
      </div>

    </div>
  </div>
</div>

<%= render "shared/footer" %>
